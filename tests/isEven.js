function isEven(number) {
    return number % 2 === 0;
}

// Example usage:
const num = 0;
if (isEven(num)) {
    console.log(`${num} is an even number.`);
} else {
    console.log(`${num} is not an even number.`);
}
